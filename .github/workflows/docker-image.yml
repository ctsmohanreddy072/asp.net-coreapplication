name: Docker Image CI

on:
  push:
    branches: [ "patch-1" ]


jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag app-test:$(date)
    - name: Docker Login
      uses: docker/login-action@v3.3.0
      with:
        username: mohanreddy072@gmail.com
        password: Narasimha60@
    - name: List docker images
      run: docker images
    - name: Tag docker image
      run: docker tag app-test:$(date) mohanreddy072/app-test:$(date)
    - name: Push docker image
      run: docker push mohanreddy072/app-test:$(date)
      
