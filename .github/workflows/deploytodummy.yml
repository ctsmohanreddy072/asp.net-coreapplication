name: deploy to prod

on:
  workflow_dispatch:
    inputs:
      build_number:
        description: 'Build number (e.g., 51)'
        required: true
        type: string

permissions:
  actions: read
  contents: read
