name: deploy to test

on:
  workflow_dispatch:
    #inputs:
      #build_number:
        #description: 'Build number (e.g., 51)'
        #required: true
        #type: string
      inputs:
       environment:
        description: 'Environment'
        required: true
        default: test
        type: choice
        options:
        - test

permissions:
  actions: read
  contents: read

jobs:
  #download:
    #runs-on: windows-latest
    #steps:
      #- name: Find and download artifact
        #uses: dawidd6/action-download-artifact@v7
        #with:
          #workflow: build.yml
          #run_number: ${{ github.event.inputs.build_number }}
          #name: drop-${{ github.event.inputs.build_number }}
          #path: ./downloaded

  Deploy_to_test:
     runs-on: windows-latest  # For a list of available runner types, refer to
                            # https://help.github.com/en/actions/reference/workflow-syntax-for-github-actions#jobsjob_idruns-on
     #needs: download
     environment: test
     steps:

    # - name: Checkout NetApp folder
     #  uses: actions/checkout@v4
     #  with:
      #  sparse-checkout: |
      #   aspnetapp     
      #  sparse-checkout-cone-mode: false

     - name: Checkout code (sparse)
       uses: actions/checkout@v4
       with:
         sparse-checkout: |
           aspnetapp
         sparse-checkout-cone-mode: false

     #- name: Ensure clean sparse checkout
      # shell: powershell
     #  run: |
     #   git sparse-checkout disable
     #   git sparse-checkout init --cone
   #     git sparse-checkout set aspnetapp
#        git read-tree -mu HEAD

   #  - name: Verify checkout contents
   #    shell: powershell
   #    run: |
      #  Write-Host "Current directory contents:"
      #  Get-ChildItem -Recurse -Depth 2 | Select-Object FullName
      #  Write-Host "`nGit sparse-checkout configuration:"
       # git sparse-checkout list
        
     - name: List files
       run: |
         dir "${{ github.workspace }}"     

     #- name: List files
      # run: dir
     #  shell: cmd
      
     - name: Test Deployment
       run: |
          echo "Deploying to Test environment"
      
